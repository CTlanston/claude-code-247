# Cycle 20260512-050542 PLAN — Track M3 (Failure clustering)

## Target dimension
M (Memory)

## Specific gap being closed
M-dim L6 requires "failure clustering script runs green" per L7 §9.
Today no clustering script exists. Build it.

## Change being made

1. `scripts/cluster_failures.py`:
   - Read `FAILURES.md`, parse entries (reuse `preflight_failures.parse_failures`)
   - Cluster entries by keyword Jaccard similarity (threshold default 0.20)
   - Output `reports/failure-clusters.md` (human-readable) and
     `reports/failure-clusters.json` (machine)
   - Each cluster: cluster id, member FAIL ids, shared keywords, suggested
     "pattern name" derived from the most common keyword across members

2. `tests/test_cluster_failures.py` ≥ 7 tests:
   - Jaccard helper correctness
   - Singleton clusters (one entry, no kin)
   - Pair clusters (two entries share keywords)
   - Multi-cluster (multiple disjoint pairs)
   - Empty input
   - --threshold flag changes grouping
   - Real-repo integration smoke

3. Run the script against the real FAILURES.md and commit the resulting
   `reports/failure-clusters.md` so it's an artifact compute_level can
   see for M-L6.

## Acceptance criteria
- [ ] `scripts/cluster_failures.py` exists
- [ ] `tests/test_cluster_failures.py` has ≥ 7 tests, all green
- [ ] `reports/failure-clusters.md` exists and is generated by the script
- [ ] `pytest -q` full suite green; no regression
- [ ] `LEVEL.md` reports `M = 6` after this cycle
- [ ] `compute_level --check` exits 0

## Files to touch (closed set)
- `scripts/cluster_failures.py` (new)
- `tests/test_cluster_failures.py` (new)
- `reports/failure-clusters.md` (new, generated)
- `cycles/20260512-050542/*` (PLAN/RESULT/REPORT/STATE.before/verify-output/proposal)
- `BACKLOG.md`, `STATE.md`, `CHANGELOG.md`, `LEVEL.md`

## Forbidden
- secrets, hand-edit LEVEL.md, existing tests, production code in
  orchestrator/autodev/runner

## Rollback
`git reset --hard autoevo/pre-20260512-050542`

## Risk score
low — additive tooling + tests; no production code.

## FAILURES.md pre-flight
Run after writing.

## Open questions
None.
