# Cycle 20260513-145929 PLAN — Cycle 39 (gitignore health artifacts)

## Target dimension

S (Safety gates). Closes a launchd-path footgun left over from
Cycle 37: tracking health.json + health.md + health.history.jsonl
in git means that any wake that re-runs `autodev_health.sh`
dirties the working tree (mtime + ts in payload), causing the
next wake's §4 step 1 "git status clean" check to fail and
write BLOCKED.md.

No level move (S at L7 max). Streak bump only (19→20).

## Specific gap being closed

Cycle 37 committed reports/health.json + health.md +
health.history.jsonl as if they were source. They're not —
they're regenerated artifacts (one row appended on each
`autodev_health.sh` call). The existing `.gitignore` already
ignores similar regenerated-per-cycle artifacts:
- reports/heartbeat.json
- reports/state.corrupt.*.json
- reports/codex-reviews.jsonl
- reports/adversarial-reviews.jsonl

The health files belong in the same category. Per Cycle 33's
discipline rule (verify before relying), the FAIL-0009-class
side-effect was avoidable here: anything that
"`run` → `update on-disk artifact`" should not be tracked.

## Change being made

1. **`.gitignore`** (+5 lines):
   Add a new section for the health artifacts following the
   existing pattern of comment + path:
   ```gitignore
   # Health-score artifacts (regenerated by orchestrator/health.py
   # / scripts/autodev_health.sh on every call). The wake script
   # will re-emit these per dispatch cycle; tracking them would
   # cause every wake to dirty the tree and fail the §4 step 1
   # "git status clean" check (FAIL-0009 class). Operational-only.
   reports/health.json
   reports/health.md
   reports/health.history.jsonl
   ```

2. **Untrack the 3 files** via `git rm --cached` (keep on disk).

3. **`tests/test_health_gitignored.py`** (new, ~5 tests):
   - `.gitignore` contains all 3 health filenames
   - `git ls-files reports/health.json` returns empty (untracked)
   - Same for health.md and health.history.jsonl
   - Wake-script simulation: run `bash scripts/autodev_health.sh`
     against a fresh git working tree; verify `git status
     --porcelain` shows NO modified entries afterwards (the
     files are gitignored, so git doesn't see the mods).

## Acceptance criteria

- [x] All 3 paths added to `.gitignore`
- [x] `git ls-files reports/health.{json,md,history.jsonl}` empty
- [x] `tests/test_health_gitignored.py` ≥ 4 tests, all green
- [x] After running `bash scripts/autodev_health.sh`,
      `git status --porcelain` returns empty
- [x] `pytest tests/ -q` green
- [x] `compute_level --check` green (after propose-first)
- [x] `autodev_doctor.sh`: 14/0/2 (unchanged)

## Files to touch (closed set)

- `.gitignore` (+5 lines)
- `tests/test_health_gitignored.py` (new)
- `CHANGELOG.md` (one line)
- `STATE.md` (rewrite)
- `cycles/20260513-145929/PLAN.md` (this)
- `cycles/20260513-145929/REPORT.md`
- `cycles/20260513-145929/RESULT.md`
- `cycles/20260513-145929/next-track-proposal.json`
- `reports/zero-deadlock-streak.txt` (19→20)
- `reports/cycle-history.jsonl` (append)

PLUS: the 3 deletions from git tracking
(`git rm --cached reports/health.{json,md,history.jsonl}`).
These show as `D` in `git status` until staged + committed.
They remain on disk.

## Files forbidden to touch

- `.env*`, `secrets/**`, `LEVEL.md`, anything in §0.
- `orchestrator/health.py` — Cycle 37; this cycle is pure
  metadata.

## Rollback plan

`git reset --hard autoevo/pre-20260513-145929`.

## Risk score

low. Untracking 3 regenerated files + gitignoring them.
Surgical and reversible.

## FAILURES.md pre-flight result

Keywords: gitignore, untrack, health, dirty, working-tree.

- No FAILURES.md matches directly. The cycle is implicitly
  Cycle-33-discipline (FAIL-0009 was about working-tree-
  dirty from pytest; this is the same pattern emerging in
  the wake-script integration of Cycle 37's health files).
  Cycle 33's lesson preserves perfectly.

## Open questions / blockers

None.
