// Stage A.3 skeleton — Approval gateway (v2). Distinct directory from the
// legacy `approval.ts` to keep that v1 module untouched per Surgical Changes.
// Filled by Stage D (Dual-Rail Dispatch + Tailscale transport).
export const APPROVAL_V2_STAGE = 'A.3-skeleton' as const
