# Cycle 20260512-042701 PLAN — BOOTSTRAP (Cycle 0)

## Target dimension
ALL six (M, S, R, C, T, E) — Bootstrap establishes the L3 baseline across the
entire rubric per §15. This is the only cycle exempt from the
"one-dimension-per-cycle" rule.

## Specific gap being closed
No L7 memory architecture exists. `LEVEL.md` is absent, so the system has no
way to compute its current rubric position or detect regressions. There are
no ADRs documenting the V4 fixes that should be locked in as L3 evidence,
and no `FAILURES.md` to inject into future PLANs.

## Change being made
Per §15 Bootstrap protocol, create the L7 memory architecture from scratch:
CONTEXT.md, docs/adr/0000-0004, FAILURES.md (seeded V3 #14/#15/#16),
BACKLOG.md, STATE.md, CHANGELOG.md, and build `scripts/compute_level.py`
+ regression tests. Run the script to generate the inaugural LEVEL.md.

## Acceptance criteria
- [ ] `LEVEL.md` exists and is generated by `scripts/compute_level.py`
      (never hand-written) with all 6 dims at >= L3
- [ ] `scripts/compute_level.py --check` exits 0
- [ ] `scripts/compute_level.py` has its own regression test suite
      (>= 8 tests, covering each dim's level logic)
- [ ] `pytest -q` green (the full existing suite plus the new
      compute_level tests; no existing test regresses)
- [ ] `./scripts/autodev_doctor.sh` exits 0 (warns allowed)
- [ ] CONTEXT.md exists and documents the 6 invariants in §5
- [ ] FAILURES.md exists with at least 3 entries (V3 #14, #15, #16)
- [ ] docs/adr/ contains ADR-0000 (pre-bootstrap judgment) through ADR-0004
      (V4 STATE_DIR resolution), all numbered sequentially
- [ ] BACKLOG.md has Track M as P0, plus initial items for S/R/C/T/E
- [ ] STATE.md follows §5 YAML schema
- [ ] CHANGELOG.md has the bootstrap entry marked with 🎯 (level-up event)
- [ ] All artifacts committed on branch `autoevo/cycle-0/bootstrap`

## Files to touch (closed set)
- `CONTEXT.md` (new)
- `FAILURES.md` (new)
- `BACKLOG.md` (new)
- `STATE.md` (new)
- `CHANGELOG.md` (new)
- `LEVEL.md` (new — generated, never hand-edited)
- `docs/adr/0000-pre-bootstrap-commit-judgment.md` (new)
- `docs/adr/0001-billable-cost-at-insert.md` (new)
- `docs/adr/0002-preflight-impossible-spec.md` (new)
- `docs/adr/0003-tdd-intent-not-strict-order.md` (new)
- `docs/adr/0004-state-dir-resolution.md` (new)
- `scripts/compute_level.py` (new)
- `tests/test_compute_level.py` (new)
- `cycles/20260512-042701/PLAN.md` (this file)
- `cycles/20260512-042701/RESULT.md` (new)
- `cycles/20260512-042701/REPORT.md` (new)
- `cycles/20260512-042701/STATE.before.md` (new — blank, pre-bootstrap state)
- `cycles/20260512-042701/verify-output.txt` (new)

## Files forbidden to touch
- `.env*`, `secrets/**`, `*.key`, `*.pem`, `id_rsa*`
- `orchestrator/main.py`, `orchestrator/billable.py`, `orchestrator/preflight.py`,
  `orchestrator/db.py`, `runner/roles/reviewer.md`, `orchestrator/roles/reviewer.md`,
  `autodev/supervisor.py` (V4 code is frozen baseline for this cycle)
- existing tests (tests/test_*.py except the new test_compute_level.py)
- `LEVEL.md` by hand — generated only

## Rollback plan
`git reset --hard autoevo/pre-20260512-042701` (this clean baseline is the
3-commits-back state on `main`).

## Risk score
low — Bootstrap creates new memory files and a single new script with its own
regression suite. No production code is touched. Worst case: rollback tag,
re-attempt with smaller scope.

## FAILURES.md pre-flight result
Not applicable (FAILURES.md does not exist yet — this cycle creates it).
Future cycles MUST run the grep step.

## Open questions / blockers
None. Proceeding.
